# 安装向导自定义说明

## 📍 当前欢迎页面位置

当前的安装向导欢迎页面由 **NSIS**（Nullsoft Scriptable Install System）自动生成，配置在：

- `DHC_Frontend/build/installer.nsh` - NSIS 安装脚本
- `DHC_Frontend/electron-builder.yml` - electron-builder 配置

NSIS 使用的是传统的 Windows 安装向导界面，样式相对固定。

## 🎨 用 Electron 创建自定义安装向导

### 方案一：使用 NSIS 插件显示 HTML（推荐）

NSIS 可以通过插件来显示 HTML 页面，但需要额外的插件支持。

**优点**：
- 可以完全自定义样式
- 使用 HTML/CSS/JS
- 与现有打包流程集成

**缺点**：
- 需要额外的 NSIS 插件
- 配置相对复杂

### 方案二：创建独立的 Electron 安装向导应用（最灵活）

创建一个独立的 Electron 应用作为安装向导，然后用它来执行实际的安装操作。

**实现步骤**：

1. **创建安装向导应用**

```bash
# 创建新的 Electron 应用目录
mkdir DHC_Installer_Wizard
cd DHC_Installer_Wizard
npm init -y
npm install electron --save-dev
```

2. **创建安装向导界面**

创建 `installer.html` 和相关的 CSS/JS 文件，使用你熟悉的前端技术（React、Vue 等）。

3. **实现安装逻辑**

在 Electron 主进程中实现：
- 文件复制
- 注册表操作（Windows）
- 创建快捷方式
- 卸载程序注册

4. **打包安装向导**

将安装向导打包成可执行文件，然后作为安装程序分发。

### 方案三：使用 electron-builder 的 portable 模式 + 首次启动向导

在应用首次启动时显示欢迎/设置向导，而不是在安装时。

**实现步骤**：

1. **在应用启动时检查首次运行**

```typescript
// src/main/index.ts
import { app } from 'electron'
import { existsSync, writeFileSync } from 'fs'
import { join } from 'path'

const firstRunFlag = join(app.getPath('userData'), '.first-run')

function checkFirstRun(): boolean {
  if (!existsSync(firstRunFlag)) {
    writeFileSync(firstRunFlag, '')
    return true
  }
  return false
}

app.whenReady().then(() => {
  if (checkFirstRun()) {
    // 显示欢迎/设置向导窗口
    showWelcomeWindow()
  } else {
    // 正常启动
    createWindow()
  }
})
```

2. **创建欢迎窗口**

```typescript
function showWelcomeWindow(): void {
  const welcomeWindow = new BrowserWindow({
    width: 800,
    height: 600,
    frame: false, // 无边框，完全自定义
    resizable: false,
    webPreferences: {
      nodeIntegration: true,
      contextIsolation: false
    }
  })

  welcomeWindow.loadFile('welcome.html')
  
  welcomeWindow.on('closed', () => {
    // 欢迎窗口关闭后，启动主应用
    createWindow()
  })
}
```

3. **创建欢迎页面**

使用你现有的前端技术栈创建美观的欢迎页面。

## 🚀 推荐方案：首次启动向导

考虑到你的项目已经使用 Electron + React，**推荐使用方案三**：

### 优点：
- ✅ 完全使用 Electron/React 技术栈
- ✅ 可以复用现有的 UI 组件库（Semi Design）
- ✅ 不需要额外的打包步骤
- ✅ 可以随时更新欢迎页面内容
- ✅ 用户体验更好（安装后首次启动时显示）

### 实现示例

我已经为你创建了一个示例欢迎页面：`DHC_Frontend/build/installer-welcome.html`

你可以在应用启动时加载这个页面，或者创建一个 React 组件来显示欢迎内容。

## 📝 快速实现首次启动向导

1. **在 `src/main/index.ts` 中添加首次运行检测**

2. **创建欢迎组件** `src/renderer/src/components/WelcomeWizard.tsx`

3. **在 `App.tsx` 中条件渲染欢迎向导**

这样你就可以完全用 React/TypeScript 来制作欢迎页面了！

## 🔧 如果需要真正的安装时向导

如果你确实需要在安装过程中显示自定义向导（而不是首次启动时），可以考虑：

1. **使用 Inno Setup** - 支持 Pascal 脚本和更灵活的 UI 定制
2. **使用 WiX Toolset** - 微软官方的安装程序工具，支持自定义 UI
3. **创建独立的 Electron 安装向导** - 完全用 Electron 制作

需要我帮你实现哪种方案？

