# DHC AC Installer - Windows 打包说明

本文档说明如何为 Windows 平台打包和分发 DHC AC Installer 应用。

## 📦 打包工具

本项目使用 **electron-builder** 作为打包工具，这是目前最流行和功能最完善的 Electron 应用打包工具。

### 为什么选择 electron-builder？

1. **功能完善**：支持 Windows、macOS、Linux 多平台
2. **安装程序**：自动生成专业的安装向导（NSIS for Windows）
3. **自动更新**：内置自动更新支持
4. **代码签名**：支持代码签名和公证
5. **配置灵活**：通过 YAML 配置文件轻松定制

## 🛠️ 打包前准备

### 1. 环境要求

- **Node.js** (v18+)
- **pnpm** 或 npm
- **Go** (v1.23+) - 用于编译后端
- **Windows 系统** - 用于生成 Windows 安装程序（或使用 Wine）

### 2. 安装依赖

```bash
cd DHC_Frontend
pnpm install
```

## 📋 打包步骤

### 方法一：在 Windows 系统上打包（推荐）

1. **编译 Go 后端**

```bash
cd DHC_Backend
# 编译 Windows x64 版本
go build -o main.exe cmd/main.go
# 将 .exe 重命名为 main（electron-builder 需要）
mv main.exe main
cd ..
```

2. **构建前端**

```bash
cd DHC_Frontend
pnpm run build
```

3. **打包安装程序**

```bash
pnpm run build:win
```

打包完成后，安装程序将生成在 `DHC_Frontend/dist/` 目录下：
- `DHC AC Installer-1.0.0-x64-setup.exe` - 64位安装程序
- `DHC AC Installer-1.0.0-ia32-setup.exe` - 32位安装程序（如果配置了）

### 方法二：使用打包脚本

在项目根目录运行：

```bash
chmod +x build_windows.sh
./build_windows.sh
```

**注意**：此脚本在非 Windows 系统上只能编译后端，完整的 Windows 安装程序需要在 Windows 系统上构建。

## 🎨 安装页面配置

### NSIS 安装向导特性

安装程序包含以下页面和功能：

1. **欢迎页面** - 显示欢迎信息
2. **安装位置选择** - 用户可以选择安装目录
3. **安装进度** - 显示安装进度
4. **完成页面** - 可选择立即启动应用

### 自定义安装页面

安装向导的配置在以下文件中：

- `DHC_Frontend/build/installer.nsh` - NSIS 安装脚本
  - 安装前检查（防止重复运行）
  - 安装完成后的操作（可选启动应用）
  - 卸载确认对话框

### 修改安装页面文本

编辑 `DHC_Frontend/electron-builder.yml` 中的 `nsis` 配置：

```yaml
nsis:
  artifactName: ${productName}-${version}-${arch}-setup.${ext}
  shortcutName: ${productName}
  uninstallDisplayName: ${productName}
  createDesktopShortcut: always  # 创建桌面快捷方式
  createStartMenuShortcut: true  # 创建开始菜单快捷方式
  oneClick: false  # 使用向导模式（非一键安装）
  allowToChangeInstallationDirectory: true  # 允许选择安装目录
```

## 📁 打包内容

安装程序包含以下内容：

- Electron 应用（前端）
- Go 后端可执行文件（`main`）
- 后端资源文件（`resources/`）
- 应用图标和资源

## 🔧 配置说明

### electron-builder.yml 主要配置

```yaml
appId: com.dhc.acinstaller  # 应用唯一标识
productName: DHC AC Installer  # 产品名称
version: 1.0.0  # 版本号

win:
  target:
    - target: nsis  # 使用 NSIS 安装程序
      arch:
        - x64  # 64位
        - ia32  # 32位（可选）
```

### 后端集成

后端可执行文件通过以下方式打包：

1. 在 `electron-builder.yml` 的 `files` 中指定：
   ```yaml
   files:
     - '../DHC_Backend/main'
     - '../DHC_Backend/resources/**'
   ```

2. 在 `asarUnpack` 中解包：
   ```yaml
   asarUnpack:
     - main
     - resources/**
   ```

3. 在 Electron 主进程中启动后端（`src/main/index.ts`）

## 🚀 分发建议

### 1. 文件大小优化

- 使用 `asar` 打包前端代码（electron-builder 默认启用）
- 压缩资源文件
- 移除不必要的依赖

### 2. 代码签名（可选但推荐）

为安装程序添加代码签名可以：
- 提高用户信任度
- 避免 Windows Defender 警告
- 支持自动更新

配置代码签名：

```yaml
win:
  certificateFile: path/to/certificate.pfx
  certificatePassword: your_password
  signingHashAlgorithms: ['sha256']
```

### 3. 自动更新（可选）

配置自动更新服务器：

```yaml
publish:
  provider: generic
  url: https://your-update-server.com/updates
```

### 4. 版本管理

每次发布新版本时：
1. 更新 `electron-builder.yml` 中的 `version`
2. 更新 `package.json` 中的 `version`
3. 重新打包

## 📝 常见问题

### Q: 打包时提示找不到后端可执行文件？

A: 确保在打包前已编译后端，并且文件名为 `main`（不是 `main.exe`）。

### Q: 安装程序很大？

A: Electron 应用本身较大（~100MB），这是正常的。可以通过以下方式减小：
- 使用 `electron-builder` 的压缩选项
- 移除不必要的依赖

### Q: 如何在非 Windows 系统上打包？

A: 可以使用 Wine 在 Linux/macOS 上运行 NSIS，但推荐在 Windows 系统上打包以获得最佳兼容性。

### Q: 安装后应用无法启动后端？

A: 检查：
1. 后端可执行文件是否正确打包
2. 文件权限是否正确
3. 查看应用日志确认错误信息

## 📚 参考资源

- [electron-builder 官方文档](https://www.electron.build/)
- [NSIS 文档](https://nsis.sourceforge.io/Docs/)
- [Electron 打包最佳实践](https://www.electron.build/configuration/configuration)

## 🎯 下一步

- [ ] 配置代码签名
- [ ] 设置自动更新服务器
- [ ] 创建安装程序截图
- [ ] 编写用户安装指南

