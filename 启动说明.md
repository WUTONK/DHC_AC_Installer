# DHC AC Installer 启动脚本使用说明

全仓库文档入口与 **`DHC_DEV` 语义**请以根目录 [README.md](./README.md) 为准。

## 📋 脚本列表

本项目提供了以下启动脚本：

### 🚀 启动脚本
- `start_backend.sh` - 仅启动后端服务 (Go)
- `start_frontend.sh` - 仅启动前端应用 (Electron)
- `start_all.sh` - 同时启动前后端服务

### 🛑 停止脚本
- `stop_all.sh` - 停止所有服务

## 🛠️ 环境要求

### 后端要求
- Go 1.23.3 或更高版本
- 确保已安装所有 Go 依赖

### 前端要求
- Node.js (推荐 LTS 版本)
- pnpm 包管理器

安装 pnpm：
```bash
npm install -g pnpm
```

## 📖 使用方法

### 方法一：分别启动 (推荐开发时使用)

1. **启动后端服务**
   ```bash
   ./start_backend.sh
   ```
   - 后端服务将在 `http://127.0.0.1:19810` 运行

2. **启动前端应用** (新终端窗口)
   ```bash
   ./start_frontend.sh
   ```
   - 前端应用将启动 Electron 窗口

### 方法二：一键启动

```bash
./start_all.sh
```

在使用此脚本时**若未预设 `DHC_DEV`，后端脚本会默认设为 `true`（simEnv），与方法一的后端默认一致**

这将：
- 自动检查环境
- 启动后端服务
- 启动前端应用
- 显示服务状态和日志位置
- 支持 Ctrl+C 停止

### 停止服务

```bash
./stop_all.sh
```

## 📊 服务信息

- **后端服务**: `http://127.0.0.1:19810`
- **前端应用**: Electron 桌面应用
- **日志目录**: `./logs/`
  - `backend.log` - 后端服务日志
  - `frontend.log` - 前端应用日志
  - `backend_setup.log` - 后端依赖安装日志
  - `frontend_setup.log` - 前端依赖安装日志

## 🔧 故障排除

### 常见问题

1. **权限错误**
   ```bash
   chmod +x *.sh
   ```

2. **Go 未安装**
   - 访问 [Go 官网](https://golang.org/) 下载安装

3. **Node.js 未安装**
   - 访问 [Node.js 官网](https://nodejs.org/) 下载安装

4. **pnpm 未安装**
   ```bash
   npm install -g pnpm
   ```

5. **端口被占用**
   - 后端默认端口: 19810
   - 前端默认端口: 11451
   - 使用 `lsof -i :端口号` 查看占用情况

### 查看日志

```bash
# 查看后端日志
tail -f logs/backend.log

# 查看前端日志
tail -f logs/frontend.log
```

## 💡 开发建议

### ⚠️ 调试前置准备（重要）

在进行安装逻辑的开发或调试之前，建议运行环境重置脚本，以确保虚拟 AC 目录处于干净的基线状态：

```bash
./reset_sim_env.sh
```

该脚本会将 `DHC_Backend/test/simEnv/acRoot/AC_SKELETON_HASDLC` 恢复为 `envBackup` 中的备份状态，避免旧的调试残留影响新的测试。


1. **多分终端**：`start_backend.sh` 与 `start_frontend.sh` 分开启动，便于分别看后端/前端日志。
2. **一键联调**：`start_all.sh` 同时启动后端与 Electron；未设置 `DHC_DEV` 时 DHC_DEV 将被自动设置为 `true`
3. **若要生产分支跑后端**：先 `export DHC_DEV=false` 再调用 `start_backend.sh`（或先于 `start_all.sh` 导出）。
4. **日志监控**: 开发时建议开启日志监控窗口
5.  **优雅停止**：使用 `stop_all.sh`。
6. **api生成**: 使用 `.gen_openapi_all.sh`从项目根目录可以一键复制openapi.json文件到前后端目录并分别运行它们的gen_api.sh脚本

## 📝 注意事项

- 首次运行会自动安装依赖，可能需要一些时间
- 确保网络连接正常，用于下载依赖包
- 如果遇到问题，请检查日志文件获取详细错误信息
- 脚本会自动创建 `logs` 目录存储日志文件
