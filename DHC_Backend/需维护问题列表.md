# 后端待办事项

> **定位**：仅记录**未完成**的技术债务与待办事项。已完成项请移除或归档到 `changelog/`。

---

## 未完成项

### 架构 / 重构

1. **DEMO 安装模块整合**：后端安装执行器架构中，`RunDemoMapInstall` 等实例应统一放在同一个文件中。
2. **React Router 迁移**：前端路由仍使用手动 switch-case，计划第三阶段迁移到 React Router。
3. **`VersionID` 重命名**：`installation` 中的 `VersionID` 命名有歧义（实际是集合 ID），建议改为 `EditionID` 或 `SetID`。

### 小优化

1. **`createInstallation()` switch 过大**：当前使用 case 编排安装流程，可评估是否用表驱动或策略模式简化。

---

## 已完成归档（可删除）

<details>
<summary>点击展开</summary>

- ✅ `readEnvBool` 迁移到 `infoGet/getEnvInfo.go`
- ✅ `GetServerInfo` 迁移到 `handler/ServerInfoHandler.go`
- ✅ 删除 `runDetectDemoResourcesIntegrity`，统一使用 `WithTracker` 版本
- ✅ 保留 `handler/systemInfoHandler.go`
- ✅ 删除 `handler/old_installtion.go`、`sync_progress_query_example.go`
- ✅ 删除 `OneClickInstallerContext.tsx`
- ✅ 重命名 `Databese/` → `Database/`
- ✅ 保留 `ShaderInstaller.v1.tsx`（迭代参考）

</details>
