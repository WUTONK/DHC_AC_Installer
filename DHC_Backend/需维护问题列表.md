1. 添加"OverwriteStartingDir":"content"识别功能
    -service.decompression.overrideControl-计划: [overwrite] cars/ddm_toyota_corolla_levin_ae86/ui -> /Users/wuzitong/Desktop/programming/DHC_AC_Installer/DHC_Backend/test/simEnv/acRoot/AC_SKELETON_HASDLC/Assetto Corsa/cars/ddm_toyota_corolla_levin_ae86/ui (dir=true)
    --- PASS: TestOverrideControl (0.00s)

重构问题
---

1. *readEnvBool*等类似信息获取通用函数应该放在infoGet包，以及serverinfoGet也是
2. runDetectDemoResourcesIntegrityWithTracker和DetectDemoResourcesIntegrity()功能重复 前者是加强函数，除非有特殊情况，这种时候应该不保留旧函数
3. 后端安装执行器架构中，从目前的架构图来看不知道DEMO安装模块实例（如RunDemoMaplnstall）是不是在同一个文件，他们应该在同一个文件中
4. handler/systemInfoHandler.go不应该删除，这是未完善功能，用来处理/api/GetDiskInfo API
5. ShaderInstaller.v1.tsx 不应该删除，这是迭代过程中需要参考的旧洁面
6. getServerInfo不应该由init.go负责，从命名上来看就不合理
7. 问题标注图中的modinstall包名问题不用解决，因为此项目中通用大驼峰命名法，其他的重构新增文件例如：executor/verify_executor.go，都需要使用类似的命名法则
8. 问题标注图中问题4中的前端直接操作后端文件不用解决
9. 使用React Router

tips:你应该按照重构建议与优先级路线图中的顺序进行重构

小问题
---

- 目前的installtion/createInstallation() 使用case处理时过于庞大，采用temper编排方式，但是这种方式比较简单，可以评估一下有没有简单的优化方案

  ```go
  switch req.VersionID {
  	case "demo-resource-verify-v1":
  		go func() {
  			_ = runInstallExecutor(installID, "resource", modinstall.RunDemoResourceVerify, true)
  		}()
  	case "demo-install-v1":
  		// DEMO 安装：按类别顺序执行 core -> weather -> map -> cars。
  		go func() {
  			// 实验流程收尾：清空后端中间目录并还原 simEnv（见 modinstall.SimEnvDevInstallCleanup 注释）。
  			defer func() {
  				_ = modinstall.SimEnvDevInstallCleanup(false)
  			}()
  			if err := runInstallExecutor(installID, "core", modinstall.RunDemoCoreInstall, false); err != nil {
  				finalizeInstallTask(installID, err)
  				return
  			}
  			if err := runInstallExecutor(installID, "weather", modinstall.RunDemoWeatherInstall, false); err != nil {
  				finalizeInstallTask(installID, err)
  				return
  			}
  			if err := runInstallExecutor(installID, "map", modinstall.RunDemoMapInstall, false); err != nil {
  				finalizeInstallTask(installID, err)
  				return
  			}
  			if err := runInstallExecutor(installID, "cars", modinstall.RunDemoCarsInstall, true); err != nil {
  				finalizeInstallTask(installID, err)
  				return
  			}
  		}()
  	default:
  		// 兼容旧版本：默认只跑 CM demo。
  		go func() {
  			_ = runInstallExecutor(installID, "cm", modinstall.RunDemoCMInstall, true)
  		}()
  		// 生产环境替换为：go runInstallExecutor(installID, "cm", modinstall.RunRealCMInstall, true)
  	}
  ```

  - installtion中的 VersionID 这个命名不好，因为我们不是区分版本，可以叫 edtion id或者 setID，消除其是“x.xx版本”误导，因为我们要安装的是集合
